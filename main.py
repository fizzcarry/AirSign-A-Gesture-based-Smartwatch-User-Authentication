import numpy as np

from identity_config import *
from fastdtw import fastdtw
import numpy as np

from itertools import permutations

import random

# 创建一个示例的列表
a=np.max(np.array([1,2.11,3.2]))
print(type(a))
print(a.shape)
b=5
c=b/a
c=float(c)
print(type(b))
print(type(c))
print(c)

